https://github.com/settings/tokens
justinrestrepo_test

testing on sunday
test3-mf

commented and understanding the code my groupmates did. Did this on monday
edward

Agency.java — Run the main method and verify that it creates a Staff object and successfully
executes the payroll process for all employees.

Executive.java — Create an Executive, award a bonus using awardBonus(), and verify that pay()
returns the base pay plus the awarded bonus.

Intern.java — Create an Intern object and verify that calling pay() returns 0 since interns
are unpaid.

Staff.java — Creates a list of different staff members and verifies that the payday() method
correctly processes and prints each employee’s payment.

StaffEmployee.java — Creates a staff employee with identifying information and returns their
pay rate when the pay() method is called.

StaffMember.java — Creates a base staff member with name, address, and phone information and
provides common functionality for all employee types.

TempEmployee.java — Creates an hourly temporary employee, adds worked hours using addHours(),
and verifies that pay() correctly calculates pay based on hours worked and the pay rate.

Justin’s commit updated StaffMember.java by completing several missing parts of the
class. He made the class public, finished the constructor to properly store employee
information, implemented the toString() method, and added the pay() method. These changes
were important because they finalized the base class that other employee types depend on in
the agency payroll system.s

